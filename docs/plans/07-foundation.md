Issue #7 implementation plan

1. Add two OpenTofu roots: state-bootstrap creates a private encrypted/versioned S3 bucket, then foundation uses a separately configured S3 backend with native lockfiles. Pin OpenTofu and AWS provider and commit provider lockfiles. Explicit inputs select account, region, owner, deployment, trusted operator principal and exact Canonical Ubuntu 24.04 x86_64 AMI; verify AMI provenance via EC2 data. No live apply without the user's deployment choices.
2. Foundation creates one public VPC/subnet/IGW/default route, zero-ingress security group, minimal SSM instance role/profile, restricted assumable operator role and a numeric versioned launch template. Root gp3 encrypted/delete-on-termination, IMDSv2, public IP, sshd for agreed SSH-over-SSM. Bootstrap creates devbox user, disables password/root SSH, writes root-owned success/failure marker. Use a dedicated read-only SSM readiness document (fixed command; no general runner); SSH authentication and host verification remain #9, with no embedded keys/secrets.
3. Export manifest schema v2 (explicit rejection of old schema) containing existing scope/resource fields plus operator role, instance role, route/IGW and readiness document identifiers; image fields include Ubuntu release, Canonical owner/name provenance, root device and exact numeric template version. Bootstrap signal/development-user documented. CLI reads only JSON, never state or OpenTofu.
4. Add an injectable AWS SDK v2 deployed-resource checker gated by valid manifest and verified identity. Verify actual networking/no ingress and public route, image provenance/state/architecture, pinned template image/profile/network/root/metadata/bootstrap configuration, IAM profile/role trust and relevant attached/inline policies, readiness document, and profile instance type architecture support. Fail closed on missing/drifted resources; errors remain allowlisted and cancellation preserves timeout exit semantics. Avoid implying doctor proves IAM effective authorization; document IAM simulation and live checks.
5. Operator permissions constrain RunInstances dependencies and template/scope/request tags; creation tags only during launch; PassRole exact instance role; inventory Describe actions necessarily unscoped but region-limited; SSM SSH/readiness documents only on owned managed instances; terminate only owned scope. No instance IAM resource access beyond SSM agent transport. Explain permissions that cannot be tag/resource scoped and boundaries of additive IAM policies.
6. Add meaningful manifest, orchestration and controlled API drift/error tests, OpenTofu native plan tests with mock provider where feasible, formatting/validation and an offline test plan. Document normal credential-chain setup, AMI resolution with provenance, bootstrap/migration, state encryption/versioning/locking inspection, recovery/teardown and ongoing durable costs.
7. Run Go checks and OpenTofu format/validate/mock plan; record exact evidence and distinguish offline checks from unperformed live acceptance. Commit/push and create PR. Have a different agent review the completed PR, address findings and revalidate. If live human testing is needed, pause with concise setup/check steps and leave live acceptance explicitly pending.

Plan review (GPT-6 Astra, high effort):
- Bound initial support to us-east-2/commercial AWS, Ubuntu 24.04 x86_64 and public IPv4; deployment values remain local and live acceptance pending.
- Include every doctor read API in the operator role and paginate IAM policy lists.
- Pin readiness document version and content, and verify actual deployed user data against its exported digest.
- Export trust/inline-policy digests to check drift against the trusted manifest; canonicalize decoded JSON and preserve literal plus signs when decoding IAM responses. Reject added policy attachments.
- Keep CLI image/template pins distinct from IAM's available constraints: IAM cannot pin a numeric template version or all non-resource launch parameters.
