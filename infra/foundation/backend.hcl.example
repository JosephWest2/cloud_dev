bucket              = "REPLACE-unique-devbox-state-bucket"
key                 = "foundation/personal-dev/terraform.tfstate"
region              = "us-east-2"
allowed_account_ids = ["123456789012"]
# encrypt=true and use_lockfile=true are set in versions.tf.
# Use AWS_PROFILE in the environment, never credential values here.
